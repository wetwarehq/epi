name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  card:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Card tests
        run: PYTHONPATH=. python -m unittest tests.test_card
